<UserControl x:Class="AutoClicker.View.Steps.ClickStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Green">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--<controls:StepInfoUserControl Grid.Row="0" Step="{Binding CurrentStep, Mode=TwoWay}" />-->

        <Image x:Name="img" Grid.Row="1" />
        <Button Content="Take Picture" Grid.Row="2" Click="TakePicture_OnClick" />

    </Grid>
</UserControl>